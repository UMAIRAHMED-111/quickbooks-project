# Flask API (default in code: http://127.0.0.1:5050)
VITE_API_BASE_URL=http://127.0.0.1:5050

# If the server sets SYNC_API_SECRET, paste the same value here (sent as X-Sync-Token and Bearer).
# VITE_SYNC_TOKEN=
