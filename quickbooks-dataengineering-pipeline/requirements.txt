# Runtime dependencies (kept in sync with pyproject.toml [project.dependencies]).
# Python >=3.11

httpx>=0.27,<1
python-dotenv>=1,<2
psycopg2-binary>=2.9,<3
google-genai>=1,<2
openai>=1.40,<2
sqlglot>=26,<27
flask>=3,<4
flask-cors>=4,<6

# Tests: pip install -e ".[dev]"   or   pip install "pytest>=8,<9"
#
# Airflow (optional, same venv as this app): pip install -e ".[airflow]"   or   pip install "apache-airflow>=3,<4"
